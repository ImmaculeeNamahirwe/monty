#include "monty.h"

/**
 * m_nop - There were no operations.
 * @stack: arrow to top of stack
 * @line_number: operation's line number
 *
 * Return: nothing
 */

void m_nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
